name: Update ICS Calendar

on:
  schedule:
    - cron: '*/45 * * * *'  # 🔹 A cada 45 minutos
  workflow_dispatch:      # Permite rodar manualmente

permissions:
  contents: write
  actions: read

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install beautifulsoup4 requests ics pytz

      - name: Generate updated calendar
        run: |
          python generate_ics.py | grep -E "✅ Adicionado|📌"

      - name: Commit and push if calendar.ics changed
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          if [[ `git status --porcelain` ]]; then
            git add calendar.ics
            git commit -m "🔄 Atualiza calendar.ics automaticamente"
            git push
          else
            echo "Nenhuma alteração detectada no calendar.ics"
          fi
