name: Atualizar calendário HLTV

on:
  schedule:
    - cron: '*/30 * * * *'  # A cada 30 minutos
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Atualizar branch local com a main remota
        run: git pull origin main

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install ics requests pytz

      - name: Executar script generate_ics.py
        run: python generate_ics.py

      - name: Verificar mudanças no calendar.ics
        run: |
          echo "📂 Status do Git:"
          git status
          echo "🔍 Diferenças no calendar.ics:"
          git diff calendar.ics || true

      - name: Commit e publicar calendar.ics (commit forçado)
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add calendar.ics

          # Commit normal ou, se não houver mudanças, commit vazio para forçar push
          git commit -m "Atualizar ICS HLTV (commit forçado)" || git commit --allow-empty -m "Commit vazio para forçar atualização ICS HLTV"

          git push
