name: Atualizar calendário HLTV

on:
  schedule:
    # Todo dia às 06:00 UTC (~03:00 Brasília)
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checar código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install ics requests pytz

      - name: Gerar calendar.ics apenas com times brasileiros
        run: |
          python <<'EOF'
          import requests
          from ics import Calendar

          BRAZILIAN_TEAMS = ["FURIA", "paiN", "MIBR", "Imperial", "Fluxo", "O PLANO", "Sharks", "RED Canids"]

          # Baixar ICS oficial do HLTV.Events
          url = "https://calendar.hltv.events/events.ics"
          response = requests.get(url)
          response.raise_for_status()

          source_calendar = Calendar(response.text)
          my_calendar = Calendar()

          for event in source_calendar.events:
              if any(team in event.name for team in BRAZILIAN_TEAMS):
                  my_calendar.events.add(event)
                  print(f"✅ Adicionado: {event.name} em {event.begin}")

          # Salvar ICS filtrado
          with open("calendar.ics", "w", encoding="utf-8") as f:
              f.writelines(my_calendar.serialize_iter())

          print("🔹 calendar.ics gerado com sucesso!")
          EOF

      - name: Configurar Git
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit e publicar calendar.ics
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          # Entrar na branch gh-pages (cria se não existir)
          git checkout gh-pages || git checkout --orphan gh-pages

          # Adicionar e commitar apenas calendar.ics
          git add calendar.ics
          git commit -m "Atualizar ICS HLTV" || echo "Nada para commitar"

          # Push para gh-pages
          git push https://$PAT_TOKEN@github.com/${{ github.repository }}.git gh-pages

          # Voltar para main
          git checkout main
