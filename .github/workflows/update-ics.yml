name: Atualizar Calendário CS2

on:
  schedule:
    - cron: "*/45 * * * *"  # 🔹 A cada 45 minutos
  workflow_dispatch:        # 🔹 Permite execução manual

permissions:
  contents: write
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install ics requests pytz beautifulsoup4 lxml

      - name: Executar script generate_ics.py
        run: python generate_ics.py

      - name: Commit e publicar calendar.ics
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add calendar.ics
          git commit -m "Atualizar ICS Liquipedia - $(date -u +"%Y-%m-%dT%H:%M:%SZ")" || echo "Sem mudanças para commitar"
          git push
