name: Atualizar calendário HLTV

on:
  schedule:
    - cron: '*/30 * * * *'  # 🔹 A cada 30 minutos
  workflow_dispatch:          # 🔹 Permite execução manual

permissions:
  contents: write
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 🔹 Checkout do repositório completo
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      # 🔹 Configurar Python 3.11
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      # 🔹 Instalar dependências Python
      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install ics requests pytz beautifulsoup4 lxml selenium

      # 🔹 Instalar Google Chrome
      - name: Instalar Google Chrome
        run: |
          sudo apt update
          sudo apt install -y wget unzip
          wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt -f install -y

      # 🔹 Instalar Chromedriver
      - name: Instalar Chromedriver
        uses: nanasess/setup-chromedriver@v1
        with:
          chrome-version: stable

      # 🔹 Executar script de atualização do ICS (mostrar todos os logs)
      - name: Executar script generate_ics.py
        run: |
          python generate_ics.py

      # 🔹 Commit automático apenas se houver mudanças
      - name: Commit e publicar calendar.ics
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add calendar.ics
          git commit -m "Atualizar ICS HLTV - $(date -u +"%Y-%m-%dT%H:%M:%SZ")" || echo "Sem mudanças para commitar"
          git push
